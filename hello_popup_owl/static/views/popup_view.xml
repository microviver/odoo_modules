<odoo>
    <template id="hello_popup_owl_template" inherit_id="website.layout" name="Hello Popup OWL Integration">
        <xpath expr="//body" position="inside">
            <script type="module">
                import { showPopup } from '@hello_popup_owl/js/popup_service'; // Adjust path based on your module structure
                // You can add logic here to determine when to show the popup, e.g.,
                // if (!sessionStorage.getItem('popupShown')) {
                //     showPopup();
                //     sessionStorage.setItem('popupShown', 'true');
                // }
                showPopup(); // For immediate display
            </script>
        </xpath>
    </template>
</odoo>